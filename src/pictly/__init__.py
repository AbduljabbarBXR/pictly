"""Pictly: image toolkit for agents. Zero Python dependencies.

Native PNG engine for exact pixel work (trim, pad, resize) plus an ffmpeg
adapter for every other format (jpeg, webp, avif) and heavy lifting.
"""

__version__ = "0.1.0"
